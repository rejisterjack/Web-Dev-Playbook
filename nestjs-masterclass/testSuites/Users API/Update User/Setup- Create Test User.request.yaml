description: Create a test user to use for UPDATE request
id: 768e8e88-25c3-4dab-b93d-fe5af1f0dfdf
name: 'Setup: Create Test User'
request:
  url:
    raw: '{{baseUrl}}/users'
    host:
      - '{{baseUrl}}'
    path:
      - users
  method: post
  headers:
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    modeOfBody: JSON
    raw: '{}'
events:
  - listen: test
    script:
      type: javascript
      exec:
        - 'pq.test(''Setup: User created successfully'', function () {'
        - '    pq.response.to.have.status(201);'
        - '});'
        - const response = pq.response.json();
        - pq.variables.set('updateUserId', response.id);
      src: ''
      packages: []
type: request
