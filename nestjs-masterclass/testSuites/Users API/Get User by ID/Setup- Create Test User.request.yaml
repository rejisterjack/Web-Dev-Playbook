description: Create a test user to use for GET request
id: 72e1ef67-eeef-4c46-9e8d-c1afd0a25b13
name: 'Setup: Create Test User'
request:
  url:
    raw: '{{baseUrl}}/users'
    host:
      - '{{baseUrl}}'
    path:
      - users
  method: post
  headers:
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    modeOfBody: JSON
    raw: '{}'
events:
  - listen: test
    script:
      type: javascript
      exec:
        - 'pq.test(''Setup: User created successfully'', function () {'
        - '    pq.response.to.have.status(201);'
        - '});'
        - const response = pq.response.json();
        - pq.variables.set('testUserId', response.id);
      src: ''
      packages: []
type: request
