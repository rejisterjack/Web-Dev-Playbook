id: b4a25853-6555-49b9-8c44-64832d39fcf3
name: Delete User Request
request:
  url:
    raw: '{{baseUrl}}/users/{{userId}}'
    host:
      - '{{baseUrl}}'
    path:
      - users
      - '{{userId}}'
  method: delete
  headers: []
  body:
    mode: none
events:
  - listen: test
    script:
      type: javascript
      exec:
        - pq.test('Status code is 200', function () {
        - '    pq.response.to.have.status(200);'
        - '});'
      src: ''
      packages: []
type: request
