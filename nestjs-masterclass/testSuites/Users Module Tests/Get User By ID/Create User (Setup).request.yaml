id: e8b7f998-2704-4421-a8e4-e429f25c074f
name: Create User (Setup)
request:
  url:
    raw: '{{baseUrl}}/users'
    host:
      - '{{baseUrl}}'
    path:
      - users
  method: post
  headers:
    - key: Content-Type
      value: application/json
  body:
    mode: raw
    modeOfBody: JSON
    raw: |-
      {
        "email": "testuser_get@example.com",
        "name": "Test User Get"
      }
events:
  - listen: test
    script:
      type: javascript
      exec:
        - pq.test('Status code is 201', function () {
        - '    pq.response.to.have.status(201);'
        - '});'
        - ''
        - pq.test('Extract User ID', function () {
        - '    const jsonData = pq.response.json();'
        - '    pq.variables.set(''userId'', jsonData.id);'
        - '});'
      src: ''
      packages: []
type: request
